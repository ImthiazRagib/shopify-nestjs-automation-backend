<!DOCTYPE html>
<html>
<head>
  <title>Welcome</title>
</head>
<body>
  <h1>Hello <%= name %> 👋</h1>
  <p>Welcome to your NestJS + EJS app!</p>
  <button id="callApiBtn">Call API</button>

  <script>
    document.getElementById('callApiBtn').addEventListener('click', async () => {
      console.log("🚀 ~ clicked:")
      try {
        const response = await fetch(`https://florida-unhabituated-gruntingly.ngrok-free.dev/api/shopify-o-auth/install?shop=quickdropx-2.myshopify.com`); // Adjust the endpoint as needed
        const data = await response.json();
        alert("🚀 ~ data: " + JSON.stringify(data.url));
        window.location.href = data.url || '/';
      } catch (error) {
        console.log("🚀 ~ error:", error)
        alert('API call failed: ' + error.message);
      }
    });
  </script>
</body>
</html>
